package com.scram.systems.privacyprotection.ui

import android.app.Application
import androidx.lifecycle.AndroidViewModel

/* This ViewModel is now a placeholder for future settings logic
such as handling language changes or theme selection. */
class SettingsViewModel(application: Application) : AndroidViewModel(application) {
    // Will add logic for language, about, etc. here soon.
}
